# Copy to `.env` and fill in your deployed Cloudflare Worker URL.
# Must exactly match the host listed in app.json's network permission whitelist.
VITE_WORKER_BASE_URL=https://twitter-for-evang2.YOUR-SUBDOMAIN.workers.dev
